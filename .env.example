# Safe default: all pages are noindex; contact form previews locally.
PUBLIC_SITE_ENV=staging
# Production-only, after a Turnstile widget has been configured for the domain:
PUBLIC_TURNSTILE_SITE_KEY=
